{{!-- <div class="card" style="width: 18rem; margin:3%" id="{{id}}"> --}}

  <div class="card-header">
    <a href="{{id}}">
      <h5 class="card-title">{{title}}</h5>
      <h6 class="card-subtitle mb-2 text-muted">Post by: {{user_id}},<br /> on: {{createdAt}} </h6>
    </a>
  </div>
 
  {{!-- <div class="card-body">
    <p class="card-text">{{content}}</p>
    <a href="#" class="card-link">Add a comment.</a>
  </div> --}}
{{!-- </div> --}}
